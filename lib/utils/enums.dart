

enum ListItem {
   map,
   button,
   animation,
   calculator,
   album,
   images,
   textfields
}

enum MenuItem {
  profile,
  setting,
  login,
  register,
  logout
}

enum ListType{
  listView,
  gridView
}
